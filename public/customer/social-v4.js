(()=>{
  const brands=['instagram','facebook','tiktok','linkedin','youtube'];
  const names={instagram:'Instagram',facebook:'Facebook',tiktok:'TikTok',linkedin:'LinkedIn',youtube:'YouTube'};
  const nitroCampaign=[
    {text:'Five tabs do not make a marketing system.',mediaUrl:'/social/aug-2026/01-one-workspace.jpg',mediaType:'image'},
    {text:'Quick question: does your website create the next conversation',mediaUrl:'/social/aug-2026/02-website.jpg',mediaType:'image'},
    {text:'The content chain:',mediaUrl:'/social/aug-2026/03-content.jpg',mediaType:'image'},
    {text:'Your future self does not want to remember',mediaUrl:'/social/aug-2026/04-social.jpg',mediaType:'image'},
    {text:'Opened is curiosity. Clicked is intent.',mediaUrl:'/social/aug-2026/05-outreach.jpg',mediaType:'image'},
    {text:'$0 to start. No card. No forced demo.',mediaUrl:'/social/aug-2026/06-start-free.jpg',mediaType:'image'},
  ];
  let queued=false,activePlatform='instagram',calendarMediaLoaded=false;
  const brandFromText=value=>{const s=String(value||'').toLowerCase();return brands.find(b=>s.includes(b))||''};
  const num=value=>{const m=String(value||'').match(/\d+/);return m?Number(m[0]):0};
  const safe=value=>String(value||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));
  const norm=value=>String(value||'').toLowerCase().replace(/\s+/g,' ').trim();
  function setBrand(el,brand){if(el&&brand)el.dataset.brand=brand}
  function clearBrands(page){page.querySelectorAll('[data-brand]').forEach(el=>el.removeAttribute('data-brand'));}
  function brandDedicatedHosts(page){page.querySelectorAll('.social-account-card').forEach((card,i)=>setBrand(card.querySelector('.social-platform-mark'),brandFromText(card.textContent)||brands[i]));page.querySelectorAll('.calendar-platform').forEach(el=>setBrand(el,brandFromText(el.getAttribute('title')||el.textContent)));page.querySelectorAll('.social-queue-card').forEach(card=>setBrand(card.querySelector('.social-queue-meta'),brandFromText(card.textContent)));}
  function addDays(calendar){const wrap=calendar.querySelector('.social-calendar-days');if(!wrap)return;const articles=[...wrap.children].filter(el=>el.tagName==='ARTICLE');articles.slice(7).forEach(article=>article.remove());[...wrap.children].filter(el=>el.tagName==='ARTICLE').forEach((article,index)=>article.dataset.dayIndex=String(index));wrap.style.gridTemplateColumns='repeat(7,minmax(0,1fr))';}
  function createStats(page,toolbar,calendar,queue){page.querySelectorAll('.social-v2-stats,.social-exact-stats').forEach(x=>x.remove());const upcoming=calendar.querySelectorAll('.calendar-post').length,cards=[...queue.querySelectorAll('.social-queue-card')],failed=cards.filter(x=>/failed|needs review|error/i.test(x.textContent)).length,connected=page.querySelectorAll('.social-account-card.is-connected').length,next=calendar.querySelector('.calendar-post em')?.textContent?.trim()||'—';const section=document.createElement('section');section.className='social-exact-stats social-v4-stats';section.innerHTML=`<article class="social-exact-stat"><span class="social-exact-stat-icon"></span><div><small>Upcoming</small><strong>${upcoming}</strong><em>Next seven days</em></div></article><article class="social-exact-stat"><span class="social-exact-stat-icon"></span><div><small>Next publish</small><strong>${safe(next)}</strong><em>Exact scheduled time</em></div></article><article class="social-exact-stat"><span class="social-exact-stat-icon"></span><div><small>Connected</small><strong>${connected}/5</strong><em class="good">Verified channels</em></div></article><article class="social-exact-stat ${failed?'has-alert':''}"><span class="social-exact-stat-icon"></span><div><small>Needs attention</small><strong>${failed}</strong><em>${failed?'Review before publishing':'Queue is healthy'}</em></div></article>`;toolbar.insertAdjacentElement('afterend',section);}
  function browserMarkup(){return `<section class="social-platform-browser glass-card" id="social-platform-browser"><div class="platform-browser-head"><div><span class="eyebrow">PLATFORM POSTS</span><h3>Browse posts by platform</h3><p>Choose a channel to see its posts, creative previews, and results in one place.</p></div><button class="btn btn-sm" type="button" data-platform-refresh>Refresh</button></div><div class="platform-browser-tabs">${brands.map(b=>`<button type="button" data-platform-tab="${b}" class="${b===activePlatform?'active':''}"><span data-brand="${b}"></span>${names[b]}</button>`).join('')}</div><div class="platform-browser-summary" id="platform-browser-summary"></div><div class="platform-browser-posts" id="platform-browser-posts"><div class="platform-browser-loading">Loading ${names[activePlatform]} posts…</div></div></section>`;}
  function metric(label,value){const clean=value!==null&&value!==undefined&&value!==''&&Number.isFinite(Number(value))?Number(value).toLocaleString():'—';return `<span><small>${label}</small><b>${clean}</b></span>`}
  function mediaPreview(group,variant,title){const source=group.thumbnailUrl||variant.thumbnailUrl||group.mediaUrl||variant.mediaUrl||'';const mediaType=String(group.mediaType||variant.mediaType||'').toLowerCase();if(!source)return `<div class="platform-post-thumb is-fallback" data-brand="${safe(variant.platform||activePlatform)}"><span>${safe(title.slice(0,1).toUpperCase())}</span></div>`;if(mediaType==='video'||mediaType==='reel')return `<div class="platform-post-thumb is-video"><video src="${safe(group.mediaUrl||variant.mediaUrl||source)}" poster="${safe(group.thumbnailUrl||variant.thumbnailUrl||'')}" muted playsinline preload="metadata"></video><i>▶</i></div>`;return `<div class="platform-post-thumb"><img src="${safe(source)}" alt="${safe(title)} preview" loading="lazy"></div>`;}
  function postRow(group,platform){const variants=group.platforms||[],variant=variants.find(v=>String(v.platform).toLowerCase()===platform)||variants[0]||{};const title=group.title||String(group.text||'Untitled post').split('\n')[0].slice(0,100),date=variant.publishedAt||group.publishedAt||group.scheduledFor;const note=variant.analyticsNote?`<div class="platform-post-note">${safe(variant.analyticsNote)}</div>`:'';return `<article class="platform-post-row">${mediaPreview(group,variant,title)}<div class="platform-post-copy"><div class="platform-post-title"><span class="platform-post-logo" data-brand="${platform}"></span><b>${safe(title)}</b></div><p>${safe(String(group.text||'').slice(0,150))}</p><small>${date?new Date(date).toLocaleString():'Published content'}</small>${note}</div><div class="platform-post-metrics">${metric('Views',variant.views)}${metric('Reach',variant.reach)}${metric('Likes',variant.likes)}${metric('Comments',variant.comments)}${metric('Shares',variant.shares)}</div>${variant.permalink?`<a class="platform-post-open" href="${safe(variant.permalink)}" target="_blank" rel="noopener">Open ↗</a>`:''}</article>`;}
  async function loadPlatform(platform,force=true){activePlatform=platform;document.querySelectorAll('[data-platform-tab]').forEach(b=>b.classList.toggle('active',b.dataset.platformTab===platform));const posts=document.getElementById('platform-browser-posts'),summary=document.getElementById('platform-browser-summary');if(!posts)return;posts.innerHTML=`<div class="platform-browser-loading">Syncing fresh ${names[platform]} stats…</div>`;try{const res=await fetch(`/api/social-analytics?platform=${encodeURIComponent(platform)}${force?'&refresh=1':''}`);const data=await res.json();if(!res.ok)throw new Error(data.error||'Could not load platform posts');const t=data.totals||{};if(summary)summary.innerHTML=`${metric('Published',t.published)}${metric('Views',t.views)}${metric('Reach',t.reach)}${metric('Engagement',t.engagement)}${metric('Clicks',t.clicks)}${metric('Conversions',Number(t.leads||0)+Number(t.signups||0))}`;posts.innerHTML=(data.posts||[]).length?data.posts.map(g=>postRow(g,platform)).join(''):`<div class="platform-browser-empty"><span data-brand="${platform}"></span><b>No ${names[platform]} posts yet</b><p>Published posts will appear here.</p></div>`;}catch(err){posts.innerHTML=`<div class="platform-browser-empty"><b>${safe(err.message)}</b><p>Try refreshing this platform.</p></div>`;}}
  function buildBrowser(page){page.querySelector('.social-performance')?.classList.add('social-performance-hidden-v4');page.querySelector('.instagram-performance')?.classList.add('social-performance-hidden-v4');let browser=page.querySelector('#social-platform-browser');if(!browser){const accounts=page.querySelector('.social-account-grid');if(!accounts)return;accounts.insertAdjacentHTML('afterend',browserMarkup());browser=page.querySelector('#social-platform-browser');browser.querySelectorAll('[data-platform-tab]').forEach(btn=>btn.addEventListener('click',()=>loadPlatform(btn.dataset.platformTab,true)));browser.querySelector('[data-platform-refresh]')?.addEventListener('click',()=>loadPlatform(activePlatform,true));loadPlatform(activePlatform,true);}}
  function cleanQueue(queue){queue.classList.remove('social-queue-hidden-v4');queue.removeAttribute('aria-hidden');queue.classList.add('social-queue-visible-v8');}
  function calendarThumbMarkup(item){const url=item.thumbnailUrl||item.mediaUrl||item.imageUrl||'';if(!url)return '';const type=String(item.mediaType||'').toLowerCase();return type==='video'||type==='reel'?`<span class="calendar-post-thumb-v4 is-video"><video src="${safe(url)}" muted playsinline preload="metadata"></video><i>▶</i></span>`:`<span class="calendar-post-thumb-v4"><img src="${safe(url)}" alt="" loading="lazy"></span>`;}
  function applyCalendarMedia(calendar,items){calendar.querySelectorAll('.calendar-post').forEach(card=>{if(card.querySelector('.calendar-post-thumb-v4'))return;const cardText=norm(card.textContent);const match=items.find(item=>{const t=norm(item.text||item.title||'');return t&&((t.length>22&&cardText.includes(t.slice(0,Math.min(44,t.length))))||(cardText.length>16&&t.includes(cardText.slice(0,Math.min(32,cardText.length)))));});if(!match)return;const markup=calendarThumbMarkup(match);if(markup)card.insertAdjacentHTML('afterbegin',markup);});}
  async function hydrateCalendarMedia(calendar){if(calendarMediaLoaded){applyCalendarMedia(calendar,nitroCampaign);return;}calendarMediaLoaded=true;let items=[...nitroCampaign];try{const res=await fetch('/api/customer-data');const data=await res.json();if(res.ok){items=[...(data.user?.workspace?.socialDrafts||[]),...items];}}catch{}applyCalendarMedia(calendar,items);}
  function bindEmptyCompose(page){page.querySelectorAll('[data-open-social-compose]').forEach(btn=>{if(btn.dataset.bound)return;btn.dataset.bound='1';btn.addEventListener('click',()=>page.querySelector('[data-workspace-action="social"]')?.click());});}
  function enhance(){if(location.hash!=='#social')return;const page=document.querySelector('.socials-workspace');if(!page)return;const toolbar=page.querySelector('.socials-toolbar'),calendar=page.querySelector('.social-calendar'),queue=page.querySelector('.social-queue-primary');if(!toolbar||!calendar||!queue)return;clearBrands(page);brandDedicatedHosts(page);addDays(calendar);createStats(page,toolbar,calendar,queue);cleanQueue(queue);buildBrowser(page);bindEmptyCompose(page);hydrateCalendarMedia(calendar);const create=toolbar.querySelector('[data-workspace-action="social"]');if(create)create.textContent='+ Create Post';}
  function schedule(){if(queued)return;queued=true;requestAnimationFrame(()=>{queued=false;enhance()})}
  new MutationObserver(schedule).observe(document.documentElement,{subtree:true,childList:true});addEventListener('hashchange',schedule);addEventListener('load',schedule);schedule();
})();
